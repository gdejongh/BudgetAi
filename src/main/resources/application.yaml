spring:
  application:
    name: budgetai
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          ddl-auto: validate
          dialect: org.hibernate.dialect.PostgreSQLDialect
          format_sql: true
